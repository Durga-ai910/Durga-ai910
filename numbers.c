#include<stdio.h>
#include<conio.h>
int main()
{
    int n;
    printf("enter an integer");
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        printf("%d",i);
    }
    return 0;
}
